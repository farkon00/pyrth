include "std.porth"

const N 100 end

proc main in
  0 1 while dup N <= do
    swap over dup * + swap
    1 + 
  end drop
  
  0 1 while dup N <= do
    swap over + swap
    1 + 
  end drop
  dup *
  
  swap - print
end
